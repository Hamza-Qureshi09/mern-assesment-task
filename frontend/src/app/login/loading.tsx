import * as React from 'react';

interface ILoginLoadingProps {
}

const LoginLoading: React.FunctionComponent<ILoginLoadingProps> = (props) => {
    return <>
        signin loading....
    </>;
};

export default LoginLoading;
