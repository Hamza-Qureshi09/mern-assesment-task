import * as React from 'react';

interface ISignupLoadingProps {
}

const SignupLoading: React.FunctionComponent<ISignupLoadingProps> = (props) => {
    return <>
        signup loading....
    </>;
};

export default SignupLoading;
